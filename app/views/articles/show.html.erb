<%== @article.body %>
<% if user_signed_in? %>
  <%= link_to '編集する', edit_article_path(@article), class: "admin_link" %>
  <%= link_to '削除する', article_path(@article), method: :delete, class: "admin_link", data: { :confirm => '削除します。よろしいですか?'} %>
<% end %>
